package com.example.payment.persistence;

/**
 * Placeholder class to avoid a compile-time error from a leftover file named
 * `TransactionType.java`. The canonical enum lives in
 * `com.example.payment.persistence.enums.TransactionType` and is used by
 * entities. This class is intentionally empty and does not participate in
 * runtime behavior.
 */
public final class TransactionType {
	private TransactionType() { }
}

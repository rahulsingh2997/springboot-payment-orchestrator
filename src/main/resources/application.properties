# Dev JWT secret (local only). Change for non-dev environments.
jwt.secret=dev-secret-which-is-long-enough-for-hs256-please-change
jwt.expiration.seconds=3600

# H2 console (local)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
